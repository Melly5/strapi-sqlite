apiVersion: v1
kind: Service
metadata:
  name: {{ include "strapi.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 1337
  selector:
    app: strapi
