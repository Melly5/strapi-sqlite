kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "strapi.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
